using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
public class Bomb : Item
{
    World world;
    Entity ent;
    float knockbackAmount = 30;
    //bomb is both an active and a collectible
    // Start is called before the first frame update
    void Start()
    {

        rarity = 0;
        description = "Boom.";
        id = 0;
        isActive = true;
        onCooldown = false;
    }

    // Update is called once per frame
    void Update()
    {

    }

    //throw the bomb! 
    public override void activeAbility()
    {
        if (Input.GetButtonDown("y") && !onCooldown)
        {

        }

    }
    protected void onLand(List<CollideResult> overlaps)
    {
            CollideResult result = overlaps[i];
            Entity target = result.entity;

                Vector2 throwLocation = ((Vector2)(Camera.main.ScreenToWorldPoint(Input.mousePosition) - play.transform.position)).normalized;
                Vector2 diff = (target.Position).normalized;

                if (diff.y > 0.4f)
                {
                
                }
                else
                {
                   
                }
    }
}
*/