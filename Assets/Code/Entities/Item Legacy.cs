using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
public class Item : MonoBehaviour
{
    //rarity for collectibles is 0, then depends on item (rusty sword is 1, steel sword would be 2, some type of legendary sword would be 3 or 4)
    public int rarity;
    //a short description of the item
    public string description;
    //name of item
    public string name;
    //integer identifier for the item
    public int id;
    //whether or not the item is an active item (i.e. a bomb)
    public bool isActive;
    //if its an active item, is it on cooldown?
    public bool onCooldown;
    //if its a consumable item, how many does the player have?
    public int amount;
    //player for items
    public Player play;
    // Start is called before the first frame update

        public Item(int id, string name, string description)
    {
    }
    
    void Start()
    {
        play = GetComponent<Player>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void pickupAction(){

    }

    public virtual void activeAbility(){
       
    }

}
*/